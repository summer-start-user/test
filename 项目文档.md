# campus-app 项目文档

> 校园类移动 App 高保真交互原型集合（HTML/JS/CSS 单页原型）
> 文档生成时间：2026-09-03
> 项目根目录：`C:\Users\Summer\Desktop\UX\campus-app`

---

## 一、项目概览

`campus-app` 是一套**面向高校学生的移动端 App 交互原型**。本文档聚焦以下三个核心交付物，全部以可直接在浏览器打开的 HTML 单页原型形式交付，便于点击评审：

| 交付物 | 入口文件 | 定位 | 状态 |
|--------|----------|------|------|
| **校园助手**（主 App） | `index.html` | 校园一站式入口：课表 / 公告 / 校园服务 / 社团广场 / 树洞社区 | 完整可跑 |
| **星隅广场** | `star-plaza/Star-Corner-Plaza.html` | 校园娱乐中心：赛道 / 拼场 / 发现热榜 / 数据收集 / 我的 | 完整可跑 |
| **智慧生活**（校园帮） | `smart-living/Smart-Living.html` | 校园互助服务平台：代取 / 代买 / 接单 / 钱包 / 认证 | 完整可跑 |

技术特征：
- 纯前端、**零依赖**、单文件即可预览（CSS/JS 内联或同目录引用）。
- 移动端视口自适应（375–430px），桌面端居中显示手机外壳。
- 交互态完整：hover / active / focus-visible / disabled、底部 Tab 路由、发布表单校验、localStorage 状态留存。

---

## 二、目录树状图（仅本项目三个核心交付物）

```
campus-app/
│
├── index.html                          # 校园助手 · 主 App（240 KB，内联 CSS/JS）
│
├── star-plaza/                         # ★ 星隅广场 · 校园娱乐中心
│   ├── Star-Corner-Plaza.html          #   单文件原型（150 KB，含完整 CSS/JS，标题"星隅广场 · 校园娱乐中心"）
│   └── img/                            #   静态资源（13 张 PNG）
│       ├── avatar-1.png … avatar-5.png #   用户头像
│       ├── banner-weekend.png          #   周末活动 Banner
│       ├── pet-mascot.png              #   虚拟宠物吉祥物
│       ├── empty-state.png             #   空状态插画
│       └── track-*.png                 #   赛道图标（create/event/game/local/
│                                        #     match/offline/radio/treehole）
│
└── smart-living/                       # ★ 校园帮 · 校园互助服务平台
    ├── Smart-Living.html               #   单文件原型（130 KB，标题"校园帮 · 校园互助服务平台"）
    └── _verify2.js                     #   校验 / 自检脚本
```

---

## 三、各模块详细说明

### 1. 校园助手（`index.html`）

校园一站式主 App，包含以下屏幕（Tab 路由）：

| 屏幕 | id | 内容 |
|------|-----|------|
| 首页 | `screen-home` | 搜索、轮播 Banner、今日课表、校园公告、我的校园服务、社团广场 |
| 地图 | `screen-map` | 校园地图 / 地点导航 |
| 社区 | `screen-community` | 树洞社区：单一数据源 + 按 `data-tag` 实时过滤的分类页 |
| 帖子详情 | `screen-post` | 帖子详情 + 图片 Lightbox（多图切换） |
| 我的 | `screen-profile` | 个人中心 |
| 发布动态 | `screen-compose` | 动态发布 |
| 我的动态 | `screen-myposts` | 我的发布列表 |

特色交互：跨页悬浮导航球（通道）、图片查看器 Lightbox、社区帖子按标签实时过滤。

### 2. 星隅广场（`star-plaza/`）

| 文件 | 说明 |
|------|------|
| `Star-Corner-Plaza.html` | 单文件原型，直接双击即可评审；含完整内联样式与交互逻辑 |
| `img/` | 13 张静态资源：5 张头像、周末 Banner、吉祥物、空状态插画、8 张赛道图标 |

核心功能模块：广场首页推荐、全部赛道 / 赛道功能清单、功能详情（拼场/报名）、发现与热榜、数据收集中心（授权开关 + 趋势图）、我的、发布新局（标题必填校验）。

### 3. 智慧生活 / 校园帮（`smart-living/`）

| 文件 | 说明 |
|------|------|
| `Smart-Living.html` | 单文件原型，标题"校园帮 · 校园互助服务平台" |
| `_verify2.js` | 页面校验 / 自检脚本 |

4 个屏幕：

| 屏幕 | id | 内容 |
|------|-----|------|
| 首页（服务大厅） | `sc-home` | 代取快递 / 代买 / 帮送等求助入口 |
| 订单 | `sc-orders` | 接单广场任务大厅 / 订单列表 / 配送中 |
| 消息 | `sc-msg` | 订单沟通聊天 |
| 我的 | `sc-me` | 个人中心 / 钱包收益 / 校园认证 |

---

## 四、如何预览

无需构建，直接用浏览器打开对应 HTML 即可：

```text
campus-app/index.html                          # 主 App
campus-app/star-plaza/Star-Corner-Plaza.html     # 星隅广场
campus-app/smart-living/Smart-Living.html          # 智慧生活 / 校园帮
```

> 桌面浏览器下会自动居中并以手机外壳（≈390×844）呈现；窄屏（≤430px）下铺满全屏。

---

## 五、模块化结构（v1.1 增强）

`index.html` 仍是唯一入口，业务逻辑已按职责拆分为模块，统一挂在 `window.App` 命名空间下，加载顺序不可调整：

| 文件 | 职责 |
|------|------|
| `index.html` | 外壳（Design Token、既有 7 个屏幕、社区核心链路） |
| `app/core.js` | 工具函数 `App.util`、数据层 `App.Store`、通用组件 `App.UI`（Toast 队列 / 确认框 / 输入弹窗 / 单选 / ActionSheet / 空态 / 骨架屏） |
| `app/router.js` | 路由 `App.Router`：屏幕注册、栈式导航、系统返回键、Tab 行为、模态层 |
| `app/screens-home.js` | 首页相关：搜索、消息中心、公告、课表、校园服务、社团；内置静态数据与图标库 |
| `app/screens-community.js` | 社区增强：发布预览与校验、分享、举报、屏蔽、私信、收藏与历史同步、发布后定位 |
| `app/screens-me.js` | 我的：编辑资料、头像、收藏、历史、设置（安全/隐私/通知/通用）、数据与存储、注销 |
| `app/boot.js` | 总装配：启动路由、升级 Toast、挂载首页各区块入口 |
| `app/screens.css` | 新增屏幕与通用组件样式（复用既有 Design Token，支持深色模式） |
| `需求说明.md` | 完整功能需求基线（首页 / 社区 / 我的 全部模块） |

### 路由规则

- 一级 Tab（首页 / 地图 / 社区 / 我的）切换时清空栈；二级页面入栈。
- 返回按钮与系统返回键共用出栈逻辑；栈深为 1 时提示「再按一次退出」。
- 「发布预览」为模态层（`pushModal`），入栈但不写入 history，不产生额外返回记录。
- 从「我的发布」进入帖子详情，返回时回到「我的发布」而非社区。

### 数据持久化

新增数据统一前缀 `campus-app:`；为保持兼容，帖子、点赞、评论仍复用历史 key
（`ws_community_posts`、`post-stats::`、`campus-comments::`、`likes::`、`blockedUsers`）。
「设置 → 数据与存储」可按分组查看占用并单项清理。

### 本次补齐的能力（对应此前审查出的缺失项）

- **首页**：搜索、消息中心、轮播跳转、周课表与课程详情、公告列表与详情（已读态）、校园服务详情、社团详情与加入。
- **社区**：搜索、发布预览与校验（字数/违禁词/图片数）、分享（真实复制链接）、举报（理由+说明+24h 冷却）、屏蔽帖子（可撤销）、私信会话、关注持久化、收藏与浏览历史、发布后自动定位并高亮新帖、「最新」排序修复。
- **我的**：编辑资料、头像管理、收藏/历史/关注/获赞下钻、我的活动、设置（账号安全/隐私/通知/通用）、缓存清理、历史管理、数据导出、账号注销全流程、帮助与关于。
- **全局**：修复 Toast 在详情页外不可见的问题；移除全部 `alert` 占位；修复「心理咨询」404 死链；Tab 与可点元素补充 ARIA。
