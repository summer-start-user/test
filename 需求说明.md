# 校园助手 · 需求说明文档（PRD）

> 项目：`campus-app/项目/index.html`（校园助手主 App 高保真交互原型）
> 版本：v1.1　编写日期：2026-09-04
> 适用范围：**首页（Home）· 社区（Community）· 我的（Profile）** 三个一级页面及其全部下钻模块
> 说明：本文为功能需求基线，同时作为原型实现的验收依据。原型为纯前端零依赖实现，数据持久化于 `localStorage`。

---

## 一、产品定位与目标

### 1.1 定位

面向高校学生的**校园一站式移动入口**：以「课表 + 公告 + 校园服务」为工具属性底座，以「树洞社区」为内容与关系载体，以「个人中心」为身份与数据管理中枢。

### 1.2 目标用户

| 角色 | 特征 | 核心诉求 |
|------|------|----------|
| 在校本科生（主） | 18–23 岁，课表驱动生活，高频使用校园卡/图书馆 | 快速查课表、看通知、逛社区、二手交易 |
| 社团活跃分子 | 参与 1–3 个社团，组织活动 | 社团招新、活动报名、成员沟通 |
| 新生 | 对校园地理与流程不熟悉 | 地图导航、办事指引、求助提问 |

### 1.3 本次迭代目标

1. 打通首页各卡片的**点击闭环**，消除「看得见、点不动」的静态稿区域。
2. 补齐社区**搜索 / 发布校验 / 预览 / 分享 / 举报**能力，形成完整的内容生产与消费闭环。
3. 建设完整的「我的」体系：个人中心、设置、数据管理三大模块全部可用。
4. 建立统一的**路由栈**，支持系统返回键，保证页面间跳转可预期。

---

## 二、全局信息架构

```
校园助手 App
├─ 首页 Home ────────────────────────────────────────────
│   ├─ 全局搜索（课程/地点/公告/帖子/社团 统一检索）
│   ├─ 消息中心（通知 / 评论 / 赞 / 私信）
│   ├─ 轮播 Banner（课程 → 课表｜公告 → 公告详情｜活动 → 活动详情）
│   ├─ 今日课表 ─→ 周课表 ─→ 课程详情（含教室导航）
│   ├─ 校园公告 ─→ 公告列表 ─→ 公告详情（含已读态）
│   ├─ 我的校园服务 ─→ 服务详情（成绩 / 图书馆 / 缴费 / 校园卡）
│   └─ 社团广场 ─→ 全部社团 ─→ 社团详情（加入 / 活动）
│
├─ 地图 Map（本期不做功能扩展，仅保证可达）
│
├─ 社区 Community ───────────────────────────────────────
│   ├─ 浏览：分类流（最新/热门/学习/校园/运动/二手/活动）+ 搜索
│   ├─ 详情：帖子详情 + 图片查看器
│   ├─ 发布：表单填写 → 图片上传 → 预览 → 校验 → 发布
│   └─ 互动：点赞 / 收藏 / 评论 / 楼中楼回复 / 分享 / 举报 / 屏蔽
│
└─ 我的 Profile ─────────────────────────────────────────
    ├─ 个人中心：资料展示 / 编辑资料 / 头像管理 / 统计数据下钻
    ├─ 内容资产：我的发布 / 我的收藏 / 浏览历史 / 获赞记录 / 我的活动
    ├─ 设置 Settings
    │   ├─ 账号安全（手机号 / 密码 / 设备管理 / 登录保护）
    │   ├─ 隐私设置（在线状态 / 动态可见范围 / 黑名单 / 个性化推荐）
    │   ├─ 通知偏好（消息分类开关 / 免打扰时段 / 震动与声音）
    │   └─ 通用（深色模式 / 字号 / 语言 / 字体粗细）
    ├─ 数据管理
    │   ├─ 缓存清理（分类清理 + 占用可视化）
    │   ├─ 历史记录管理（浏览历史 / 搜索历史）
    │   └─ 数据导出与账号注销
    └─ 帮助与反馈 / 关于我们 / 退出登录
```

---

## 三、首页（Home）需求

### 3.1 功能清单

| 编号 | 功能 | 优先级 | 需求描述 |
|------|------|--------|----------|
| H-1 | 全局搜索 | P0 | 顶部搜索框点击进入搜索页，覆盖课程、地点、公告、帖子、社团五类数据源 |
| H-2 | 消息中心 | P0 | 铃铛入口，四分组（通知/评论/赞/私信），已读态与首页红点双向同步 |
| H-3 | 头像入口 | P1 | 点击直达个人中心 Tab |
| H-4 | 轮播 Banner | P1 | 三张运营位分别跳转课表、公告详情、活动详情 |
| H-5 | 课表 | P0 | 今日 3 条摘要 → 周课表（周次切换）→ 课程详情（教师/教室/周次/导航） |
| H-6 | 校园公告 | P0 | 列表 → 详情，进入即已读，未读数递减 |
| H-7 | 校园服务 | P0 | 4 张卡片分别进入成绩、图书馆、缴费、校园卡详情页 |
| H-8 | 社团广场 | P1 | 卡片 → 社团详情；「全部社团」→ 可筛选可搜索的社团列表 |

> 说明：首页由「课表 / 公告 / 校园服务 / 社团广场」四个内容区块构成，不设独立快捷入口与推荐流区块。

### 3.2 关键交互规则

- **搜索页**：进入自动聚焦；展示搜索历史（可单删/清空）与热搜榜；输入时实时联想；结果按类型分页签；无结果时展示推荐内容。
- **消息中心**：未读条目左侧有圆点，点击进入即标记已读；「一键全读」按钮；左滑删除单条；首页铃铛红点数 = 消息中心未读总数。
- **公告已读**：已读状态持久化，首页公告条目的「新」标记随之消失，「5 条未读」动态递减为实际未读数。
- **课表周视图**：7 列 × 课程序卡片网格，左右滑动切换周次，当前时段高亮并显示时间红线。

---

## 四、社区（Community）需求

### 4.1 功能清单

| 编号 | 功能 | 优先级 | 需求描述 |
|------|------|--------|----------|
| C-1 | 分类浏览 | P0 | 最新/热门/学习/校园/运动/二手/活动，横向分页 + 手势/滚轮/键盘 |
| C-2 | 搜索 | P1 | 社区内帖子检索，支持历史与结果高亮 |
| C-3 | 帖子详情 | P0 | 正文、图片查看器、评论（楼中楼）、排序、长按菜单 |
| C-4 | 发布 | P0 | 标题+正文+图片+话题+可见性；**必须支持预览与校验** |
| C-5 | 点赞 / 收藏 | P0 | 即时反馈 + 持久化 + 源卡片与详情双向同步 |
| C-6 | 评论 / 回复 | P0 | 顶级评论 + 楼中楼回复，持久化到本机 |
| C-7 | 分享 | P1 | 底部面板：复制链接 / 生成海报 / 分享到微信 / 分享到 QQ |
| C-8 | 举报 / 屏蔽 | P1 | 举报需选择理由并可补充说明；屏蔽帖子后从信息流移除且可撤销 |
| C-9 | 关注作者 | P1 | 关注状态持久化，同步到个人中心关注数 |
| C-10 | 私信 | P2 | 从作者胶囊进入会话 |

### 4.2 发布流程（重点）

```
点击发布 → 填写表单 → [预览] → 校验 → 发布成功 → 定位到新帖
                         ↑________|
```

**表单字段**

| 字段 | 必填 | 约束 |
|------|------|------|
| 发布到（板块） | 是 | 单选：校园/学习/运动/二手/活动 |
| 标题 | 否 | ≤ 40 字 |
| 正文 | 是 | 1–500 字，剔除首尾空白后校验 |
| 图片 | 否 | ≤ 9 张，支持删除、排序、设封面 |
| 话题 | 否 | 支持 `#` 输入联想与预设选择 |
| 可见性 | 是 | 公开 / 匿名 |

**校验规则**

1. 正文为空 → 发布按钮保持 disabled，点击时提示「请填写动态内容」。
2. 正文超 500 字 → 计数器变红，阻止发布。
3. 图片超 9 张 → 提前禁用图片按钮并提示「最多 9 张图片」。
4. 标题超 40 字 → 阻止输入（maxlength）+ 达到上限时提示。
5. 含违禁词（可配置词表）→ 标红并阻止发布。

**预览**：以真实 feed 卡片样式渲染（含匿名头衔、图片九宫格、话题标签、时间「刚刚」），底部提供「返回编辑 / 直接发布」双按钮。

### 4.3 关键交互规则

- **新帖定位**：发布成功后自动切换到该帖所属分类、列表滚动到顶部、新卡片高亮闪动一次。
- **分享**：复制链接需真实写入剪贴板并 toast 确认（禁止假 toast）。
- **举报**：ActionSheet 选择理由 → 提交 → toast「举报已提交」，同一帖子 24h 内不可重复举报。
- **屏蔽帖子**：二次确认 → 从所有分类页移除 → toast 提供「撤销」操作。

---

## 五、我的（Profile）需求

### 5.1 个人中心

| 编号 | 功能 | 需求描述 |
|------|------|----------|
| P-1 | 信息展示 | 头像、昵称、认证标识、等级、院系专业、个性签名 |
| P-2 | 编辑资料 | 昵称（2–16 字）、性别、年级、专业、个性签名（≤50 字）、生日；实时校验字数，保存后首页问候语同步更新 |
| P-3 | 头像管理 | 查看大图 / 从相册选择 / 使用系统头像（6 款预设）/ 移除后回退为首字母 |
| P-4 | 等级体系 | 由发布数与获赞数计算，进度条与「还需 N 条升级」文案动态生成，可查看等级规则 |
| P-5 | 统计下钻 | 发布 → 我的发布；收藏 → 收藏列表；关注 → 关注列表；获赞 → 获赞记录 |
| P-6 | 我的活动 | 我报名的 / 我参加的 分组，空态引导去社区活动分类 |
| P-7 | 校园服务入口 | 校园卡、我的成绩（复用首页服务详情） |

### 5.2 设置模块

#### 5.2.1 账号安全

| 项 | 类型 | 说明 |
|----|------|------|
| 手机号 | 跳转 | 展示脱敏号码 `138****8888`，可进入更换流程（验证码校验） |
| 登录密码 | 跳转 | 修改密码，需原密码 + 新密码（8–20 位，含字母与数字）二次确认 |
| 实名认证 | 展示 | 未认证 / 已认证状态，未认证可进入认证流程 |
| 登录设备管理 | 跳转 | 设备列表（机型/最近登录时间/地点），支持移除其他设备 |
| 两步验证 | 开关 | 开启后异地登录需短信验证 |
| 登录提醒 | 开关 | 新设备登录时推送通知 |

#### 5.2.2 隐私设置

| 项 | 类型 | 说明 |
|----|------|------|
| 在线状态可见 | 开关 | 关闭后他人看不到我的在线状态与最后活跃时间 |
| 动态可见范围 | 单选 | 所有人 / 仅本校 / 仅关注的人 / 仅自己 |
| 允许被搜索到 | 开关 | 关闭后其他用户无法通过昵称搜索到我 |
| 黑名单 | 跳转 | 已屏蔽用户列表，支持取消屏蔽 |
| 个性化内容推荐 | 开关 | 关闭后社区按时间序推荐，不做个性化排序 |
| 展示获赞与收藏数 | 开关 | 控制主页统计数据的公开性 |

#### 5.2.3 通知偏好

| 项 | 类型 | 说明 |
|----|------|------|
| 接收新消息通知 | 总开关 | 关闭后以下分项全部置灰不可操作 |
| 评论与回复 | 开关 | — |
| 点赞与收藏 | 开关 | — |
| 系统公告 | 开关 | 默认开启且不可关闭（重要通知） |
| 社团与活动 | 开关 | — |
| 免打扰时段 | 跳转 | 设置起止时间，时段内不推送 |
| 声音与震动 | 开关 | 各一项 |

#### 5.2.4 通用

| 项 | 类型 | 说明 |
|----|------|------|
| 深色模式 | 开关 | 跟随系统 / 始终浅色 / 始终深色 |
| 字号大小 | 单选 | 标准 / 大 / 超大 |
| 语言 | 单选 | 简体中文 / English |
| 清除缓存 | 跳转 | 见 5.3 |

### 5.3 数据管理

| 编号 | 功能 | 需求描述 |
|------|------|----------|
| D-1 | 缓存清理 | 分类展示占用：图片缓存 / 帖子数据 / 浏览历史 / 搜索历史 / 其他；支持单项清理与一键清理；清理前二次确认 |
| D-2 | 浏览历史 | 按时间倒序，支持单条删除、清空全部、按类型筛选；空态引导 |
| D-3 | 搜索历史 | 同上，独立于浏览历史 |
| D-4 | 数据导出 | 导出我的发布与评论为 JSON 文件，提示「仅保存在本设备」 |
| D-5 | 账号注销 | 详见 5.4 |

### 5.4 账号注销（高危流程）

```
设置 → 账号安全 → 账号注销
  → 风险告知页（列出将清除的数据清单）
  → 勾选「我已了解以上风险」
  → 输入「注销」两字进行二次确认
  → 冷静期提示（7 天内可撤销，期间登录即自动撤销）
  → 注销成功 → 清除全部本地数据 → 回到登录页
```

**规则**

- 输入框内容必须严格等于「注销」才可提交。
- 注销前若存在未完成的交易/活动报名，需提示用户先处理。
- 注销后清除 `localStorage` 中全部 `campus-app` 命名空间数据。
- 原型中提供「模拟注销」与「取消」两条路径，注销后可直接重置为初始状态以便继续评审。

### 5.5 其他

| 编号 | 功能 | 需求描述 |
|------|------|----------|
| O-1 | 帮助与反馈 | FAQ 折叠列表（账号/社区/课表/支付四类）+ 意见反馈表单（分类 + 内容 + 图片 + 联系方式） |
| O-2 | 关于我们 | 版本、更新日志、服务协议、隐私政策、开源许可 |
| O-3 | 退出登录 | 二次确认后清除会话态并回到登录页 |

---

## 六、路由与导航需求

| 编号 | 规则 | 说明 |
|------|------|------|
| R-1 | 屏幕栈 | 二级页面入栈，返回按钮与系统返回键共用出栈逻辑 |
| R-2 | Tab 行为 | 切换一级 Tab 时清空栈，Tab 之间不形成层级 |
| R-3 | 返回键 | 栈深度 > 1 时返回上一级；栈深度 = 1 时提示「再按一次退出」 |
| R-4 | Tab Bar 显隐 | 一级 Tab 页显示；二级页面隐藏（发布、详情、设置等） |
| R-5 | 来源溯源 | 从「我的发布」进入帖子详情，返回时应回到「我的发布」而非社区 |
| R-6 | 悬浮球 | 仅在一级 Tab 页（首页）显示，二级页自动隐藏 |

---

## 七、数据模型（localStorage）

命名空间统一前缀 `campus-app:`，由 `App.Store` 统一封装。

| Key | 类型 | 说明 |
|-----|------|------|
| `campus-app:profile` | Object | 昵称、性别、年级、专业、签名、头像、生日 |
| `campus-app:posts` | Array | 用户发布的帖子（含图片 dataURL） |
| `campus-app:notices-read` | Array | 已读公告 id |
| `campus-app:messages` | Array | 消息中心数据 |
| `campus-app:favorites` | Array | 收藏的帖子 id |
| `campus-app:history` | Array | 浏览历史（id + 时间戳 + 类型） |
| `campus-app:search-history` | Array | 搜索历史 |
| `campus-app:follows` | Array | 关注的用户名 / 社团 |
| `campus-app:blocked-users` | Array | 黑名单 |
| `campus-app:hidden-posts` | Array | 已屏蔽的帖子 |
| `campus-app:reports` | Object | 举报记录（帖子 key → 时间戳） |
| `campus-app:settings` | Object | 通知、隐私、通用设置 |
| `campus-app:theme` | String | light / dark / system |

---

## 八、通用状态与异常

| 场景 | 处理 |
|------|------|
| 列表为空 | 统一空态：插画 + 主文案 + 副文案 + 引导按钮 |
| 数据加载 | 骨架屏占位（列表页首屏 3–5 条） |
| 存储写入失败 | 捕获 `QuotaExceededError`，提示「存储空间不足，请清理缓存」 |
| 重复操作 | 连续点击做防抖，Toast 走队列不互相覆盖 |
| 无网络 | 原型无真实网络请求，不涉及 |

---

## 九、非功能需求

1. **零依赖**：不引入任何第三方库，HTML/CSS/JS 原生实现。
2. **模块化**：按 `core / router / screens-home / screens-community / screens-me` 拆分，统一 `App` 命名空间。
3. **可访问性**：可点元素具备 `aria-label`，开关具备 `aria-pressed`，Tab 具备 `role="tab"` 与 `aria-selected`。
4. **视觉一致**：复用既有 Design Token（色板、圆角、阴影、间距），不新增魔法数值。
5. **移动端优先**：设计宽度 375×1.15 ≈ 431px，窄屏等比缩放。

---

## 十、验收清单

- [ ] 首页所有可点元素（搜索/铃铛/头像/轮播/课表/公告/服务/社团）均有响应
- [ ] 社区可搜索、可发布（含预览与校验）、可分享、可举报
- [ ] 「我的」全部菜单项均有落地页，无 toast 占位
- [ ] 设置三模块（账号安全 / 隐私 / 通知）开关具备持久化
- [ ] 缓存清理可查看占用并可单项清理
- [ ] 账号注销走完完整高危流程
- [ ] 系统返回键在任意二级页可正常回退
- [ ] 从「我的发布」进详情后返回，回到「我的发布」
- [ ] 深浅色切换生效且持久化
- [ ] 全站无 404 死链、无 `alert` 占位
