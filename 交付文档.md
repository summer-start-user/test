# 校园助手 · 原型交付文档

> 版本：v1.2　更新日期：2026-09-04
> 适用范围：`campus-app/项目` 全部交付物（校园助手主 App、星隅广场、校园帮）
> 用途：评审 / 交付 / 验收依据。本文档面向「第一次接触本项目的评审者」，自包含全部必要信息。

---

## 一、交付物总览

| 交付物 | 入口文件 | 定位 | 状态 |
|--------|----------|------|------|
| **校园助手**（主 App） | `index.html` | 校园一站式入口：课表 / 公告 / 校园服务 / 社团 / 树洞社区 / 我的 | 完整可跑 |
| **星隅广场** | `star-plaza/Star-Corner-Plaza.html` | 校园娱乐中心：赛道 / 拼场 / 发现热榜 / 数据收集 | 完整可跑 |
| **校园帮**（智慧生活） | `smart-living/Smart-Living.html` | 校园互助服务平台：代取 / 代买 / 接单 / 钱包 | 完整可跑 |

三个原型均为**纯前端、零依赖**实现，无需构建，浏览器直接打开即可评审。

### 文件结构

```
项目/
├── index.html                 # 校园助手 · 主 App 入口（外壳 + 7 个原生屏幕）
├── app/                       # 主 App 模块化代码（挂 window.App 命名空间）
│   ├── theme.css              #   设计令牌（Design Tokens）
│   ├── screens.css            #   屏幕与组件样式（含深色模式）
│   ├── core.js                #   工具 / 数据层 / 通用组件（Toast、弹窗、ActionSheet…）
│   ├── router.js              #   路由：栈式导航、返回键、Tab、模态层
│   ├── screens-home.js        #   首页域：搜索、消息、公告、课表、服务、社团
│   ├── screens-community.js   #   社区域：发布校验、分享、举报、屏蔽、私信
│   ├── screens-me.js          #   我的域：资料、设置、数据管理、注销
│   └── boot.js                #   总装配与启动
├── star-plaza/                # 星隅广场（单文件原型）
├── smart-living/              # 校园帮（单文件原型）
├── 需求说明.md                # PRD 功能基线（v1.1）
├── 项目文档.md                # 项目概览（v1.1）
└── 交付文档.md                # 本文档
```

> ⚠️ 目录与文件名均为 ASCII。历史教训：HTTP 预览下中文路径易 404，故 `星隅广场→star-plaza`、`智慧生活→smart-living` 已重命名。

---

## 二、评审方式

无需安装任何环境：

```text
项目/index.html                              # 主 App（推荐经本地 HTTP 服务打开）
项目/star-plaza/Star-Corner-Plaza.html       # 星隅广场
项目/smart-living/Smart-Living.html          # 校园帮
```

- 桌面浏览器：自动居中呈现手机外壳（≈390×844），按移动端真实比例评审
- 窄屏（≤430px）或 DevTools 手机模拟：铺满全屏
- 深色模式：主 App「设置 → 通用 → 深色模式」切换，持久化到 localStorage
- 数据留存：所有操作（发布、点赞、已读、设置…）保存在 localStorage，可反复评审不清场

---

## 三、技术架构

### 3.1 模块与加载顺序（不可调整）

`index.html` → `theme.css` → `screens.css` → `core.js` → `router.js` → `screens-home.js` → `screens-community.js` → `screens-me.js` → `boot.js`

统一命名空间 `window.App`，各模块职责：

| 模块 | 职责 |
|------|------|
| `App.util` | 工具函数（转义、时间格式化等） |
| `App.Store` | 数据层：localStorage 统一封装（前缀 `campus-app:`） |
| `App.UI` | 通用组件：Toast 队列、确认框、输入弹窗、单选弹窗、ActionSheet、空态、骨架屏 |
| `App.Router` | 屏幕注册、栈式导航、系统返回键、Tab 行为、模态层、悬浮球显隐 |
| `App.Screens` | 各业务屏幕（render + 事件委托） |

### 3.2 视觉系统

- **设计令牌**集中在 `app/theme.css`（`--u: 1.15px` 间距单位、色板、圆角、阴影），全站复用，无魔法数值
- **深色模式**：`#app[data-theme="dark"]` 覆盖令牌变量，组件样式自动适配
- 设计宽度 375 × 1.15 ≈ 431px，窄屏等比缩放

---

## 四、屏幕清单（主 App 全量）

一级 Tab 4 个 + 二级屏幕 30+，全部可点击到达，无占位死页。

### 首页域（screens-home.js）

| 屏幕 | id | 说明 |
|------|-----|------|
| 首页 | `screen-home` | 搜索 / 消息 / 轮播 / 今日课表 / 公告 / 校园服务 / 社团广场 |
| 全局搜索 | `screen-search` | 五类数据源（课程/地点/公告/帖子/社团）统一检索、历史与联想 |
| 消息中心 | `screen-msg` | 通知 / 评论 / 赞 / 私信四分组，已读态与红点同步 |
| 周课表 | `screen-schedule` | 7 列 × 8 节网格（详见 §6.1 课表规范） |
| 课程详情 | `screen-course` | 教师 / 教室 / 周次 / 教室导航 |
| 公告列表 / 详情 | `screen-notices` / `screen-notice` | 进入即已读，未读数递减 |
| 校园服务 | `screen-service` | 成绩 / 图书馆 / 缴费 / 校园卡详情 |
| 社团列表 / 详情 | `screen-clubs` / `screen-club` | 可筛选可搜索；加入流程 |
| 活动详情 | `screen-activity` | 报名流程 |

### 社区域（screens-community.js + index.html 原生）

| 屏幕 | id | 说明 |
|------|-----|------|
| 社区 | `screen-community` | 分类流：最新/热门/学习/校园/运动/二手/活动 |
| 帖子详情 | `screen-post` | 图片查看器、楼中楼评论、长按菜单 |
| 发布 / 预览 | `screen-compose` / `screen-preview` | 表单 → 预览 → 校验 → 发布闭环 |
| 私信会话 | `screen-chat` | 从作者胶囊进入 |
| 举报 | `screen-report` | 理由 + 说明，24h 冷却 |

### 我的域（screens-me.js）

| 屏幕 | id | 说明 |
|------|-----|------|
| 个人中心 | `screen-profile` | 资料展示、统计下钻（发布/收藏/关注/获赞） |
| 编辑资料 / 头像 | `screen-edit-profile` / `screen-avatar` | 实时校验；6 款预设头像 |
| 收藏 / 历史 / 关注 / 获赞 | `screen-favs` / `screen-history` / `screen-follows` / `screen-likes` | 内容资产管理 |
| 我的活动 / 我的社团 | `screen-activity-mine` / `screen-clubs` | — |
| 设置 | `screen-settings` | 账号安全 / 隐私 / 通知 / 通用 四大分组 |
| 安全 / 设备 / 黑名单 | `screen-security` / `screen-devices` / `screen-blacklist` | — |
| 隐私 / 通知 / 通用 | `screen-privacy` / `screen-notify` / `screen-general` | 开关均持久化 |
| 缓存清理 / 历史管理 | `screen-cache` / `screen-history-manage` | 占用可视化、单项清理 |
| 举报记录 / 屏蔽帖子 | `screen-report-list` / `screen-blocked-posts` | — |
| 注销 | `screen-deactivate` | 高危流程：风险告知 → 输入「注销」→ 冷静期 |
| 帮助 / 关于 / 心理咨询 | `screen-help` / `screen-about` / `screen-psy` | FAQ 折叠、版本信息 |

---

## 五、全局交互规范

### 5.1 导航与返回（R 系列）

- **栈式导航**：二级页入栈；返回按钮与浏览器/系统返回键共用出栈逻辑
- **Tab 行为**：切换一级 Tab 清空栈，Tab 间不形成层级；栈深 1 时再按返回提示「再按一次退出」
- **模态层**：发布预览等走 `pushModal`，入栈不写 history，不产生额外返回记录
- **来源溯源**：从「我的发布」进详情，返回回到「我的发布」而非社区
- **悬浮球**：仅首页显示，二级页自动隐藏
- 「刷新当前页 / 替换栈顶」语义一律 `replaceState`（历史教训：误用 pushState 会撑爆历史栈）

### 5.2 弹窗体系与防堆叠（v1.2 最终口径）

| 控件 | 入口 | 保护规则 |
|------|------|----------|
| 确认 / 输入 / 单选弹窗 | `UI._dialog` | 已有弹窗在屏时忽略新触发（按取消处理） |
| ActionSheet / 分享面板 | `UI.sheet` | 同上 |
| Toast 消息框 | `UI.toast` | 同一条消息「正在显示或已在排队」时不重复入队 |

**设计原则**：
- 防堆叠只做「在屏 / 在队」判断，**不做时间冷却**——弹窗关闭后立即可再次打开，Toast 播完即可重播
- 保护逻辑在 `UI.*` 内部统一实现，各业务调用点零侵入
- **绝不阻塞业务动作**：返回、导航、即时操作点击立即生效；只抑制重复的「提示展示」
- Toast 串行播放（每条约 1.8s），队列保证不互相覆盖

> 历史教训：曾对弹窗加 5 秒冷却，导致「关闭后想再看一次」被吞；也曾把限频铺到所有按钮导致返回键失效。最终收敛为上表的纯防堆叠方案。

### 5.3 状态与异常

- 空列表：统一空态（插画 + 主副文案 + 引导按钮）
- 加载：骨架屏占位
- 存储写满：捕获 `QuotaExceededError` 并提示清理
- 全站无 `alert` 占位、无 404 死链

---

## 六、重点模块规范

### 6.1 周课表（v1.2 重设计，本次交付核心）

**结构**：7 列（周一~周日常显）× 8 节（每节含开始时间）CSS Grid 网格。

| 特性 | 规范 | 实现要点 |
|------|------|----------|
| 跨节课 | 课程块用 `grid-row: N / span len` 真正跨节，块体撑满整段时长 | 建占用表 `day-sec → course`，延续节不渲染 |
| 真实日期 | 表头 = 周几 + 几月几日（如「周五 9月4日」） | 学期起始日由**今天反推**（第 CUR_WEEK 周恒含今天），无硬编码，任何时候打开日期自洽 |
| 今日标识 | 今日表头为主色**实底胶囊**（白字+投影）；今日整列淡蓝底 | 仅查看本周时生效，翻周自动取消 |
| 当前节 | 正在上的节：节次列主色竖条 + 脉冲小点 | 节开始 ≤ 现在 < 下一节开始；仅本周今天计算 |
| 周末标识 | **黑白灰**：近黑周几 + 灰色日期 + 灰底列，语义即「未上课」 | 不用暖色；深色模式变量自动翻转 |
| 节次列 | 数字（12px/700/近黑）+ 等宽灰色时间 | 数字为主层级 |
| 课程块 | 五色系渐变 + 白字 + 顶部高光（窄格需饱和度撑住视觉分量） | k1 蓝 / k2 绿 / k3 橙 / k4 紫 / k5 粉；课程名 3 行截断、教室单行省略 |
| 行高 | `grid-template-rows: minmax(46u, auto)` | 显式最小高度保证跨行块撑满 |

**设计决策记录**（评审时可直接引用）：
1. **日期列常显不裁剪**——哪怕周末整列无课也保留占位。理由：调课 / 补课 / 实验课排到周末时必须立刻可见，隐藏列会丧失对临时变动的可见性。宁可列窄，不可列缺。
2. **配色范式取舍**——曾试「低饱和浅底深字」（iOS 日历风格），在约 44px 窄格中显得寡淡无层次，最终保留高饱和渐变 + 白字；后续只微调色值，不再换范式。

### 6.2 社区发布闭环

```
填写表单 → 预览（真实 feed 卡片渲染）→ 校验 → 发布 → 自动定位新帖并高亮
```

校验规则：正文 1–500 字、标题 ≤40 字、图片 ≤9 张、违禁词表、匿名可选；不满足时按钮禁用或阻断并提示。

### 6.3 账号注销（高危流程）

风险告知 → 勾选「我已了解」→ 输入「注销」二字（严格匹配）→ 冷静期提示 → 清除 `campus-app:` 全部命名空间数据。原型提供「模拟注销 / 取消」双路径便于反复评审。

---

## 七、数据模型（localStorage）

统一前缀 `campus-app:`，由 `App.Store` 封装。主要 Key：

| Key | 说明 |
|-----|------|
| `campus-app:profile` | 昵称、性别、年级、专业、签名、头像、生日 |
| `campus-app:posts` | 用户发布的帖子（含图片 dataURL） |
| `campus-app:notices-read` | 已读公告 id |
| `campus-app:messages` | 消息中心数据 |
| `campus-app:favorites` / `history` / `search-history` / `follows` | 收藏 / 历史 / 搜索 / 关注 |
| `campus-app:blocked-users` / `hidden-posts` / `reports` | 屏蔽与举报记录 |
| `campus-app:settings` / `theme` | 设置与主题 |

历史兼容：帖子、点赞、评论复用旧 key（`ws_community_posts`、`post-stats::` 等）。

---

## 八、验收清单

### 通用（v1.1 基线）

- [ ] 首页所有可点元素（搜索/铃铛/头像/轮播/课表/公告/服务/社团）均有响应
- [ ] 社区可搜索、可发布（含预览与校验）、可分享（真实复制链接）、可举报（24h 冷却）
- [ ] 「我的」全部菜单有落地页，设置开关持久化
- [ ] 缓存清理可查看占用并可单项清理
- [ ] 系统返回键任意二级页可回退；「我的发布 → 详情 → 返回」回到「我的发布」
- [ ] 深浅色切换生效且持久化；全站无 404 死链、无 alert 占位

### 课表（v1.2 新增）

- [ ] 跨节课块体连续撑满两节，无空洞、无重复渲染
- [ ] 表头显示真实日期，翻周日期正确顺延
- [ ] 今日胶囊 + 整列高亮只在查看本周时出现
- [ ] 白天打开课表，当前节有主色竖条 + 脉冲指示
- [ ] 周末列黑白灰呈现；若在数据中给周六加一门课，立即出现在周六列
- [ ] 深色模式下课表完整可读（课程块渐变 + 白字通用）

### 弹窗与提示（v1.2 最终口径）

- [ ] 弹窗打开时连点不产生堆叠
- [ ] 弹窗关闭后**立即**再点可重新打开（无冷却）
- [ ] Toast 连点同一消息只显示一次；播完后再点可正常重播
- [ ] Toast 播放期间触发不同消息正常排队

---

## 九、版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.1 | 2026-09-03/04 | 模块化拆分；补齐首页/社区/我的全部下钻闭环；PRD 基线确立 |
| v1.2 | 2026-09-04 | 课表网格重设计（跨节 span、真实日期、今日胶囊、当前节指示、周末黑白灰、五色渐变课程块）；弹窗/toast 收敛为纯防堆叠（移除时间冷却）；星隅广场 / 智慧生活目录改 ASCII 名；新增本交付文档 |
